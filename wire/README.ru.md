[English](README.md) · **Русский**

# Две машины через канал

Полный разбор от постановки задачи до двух работающих автоматов: платёжный терминал и хост авторизации, соединённые каналом, который можно разорвать, замедлить и заставить доставить сообщение дважды. Разделы идут в порядке работы — сначала два графа переходов, затем контексты, условия и операции, затем стык между машинами, канал, подключение к странице и анализ. В коде определения типов обычно стоят перед схемой; здесь они появляются по мере необходимости.

Обозначения и определения даны в [руководстве](https://github.com/evgkch/machjs/blob/master/README.ru.md). Ссылки вида «п. 4.2» указывают на разделы этого документа; на руководство ссылаемся по названию раздела — «README, „Схема переходов“».

**Рабочий проект.** Пример открывается как страница — [живая версия](https://evgkch.github.io/machjs/wire/). Vite, обычный HTML и TypeScript, без фреймворков; команды выполняются из корня этого репозитория:

```sh
npm install
npm run dev       # http://localhost:5173/wire/
npm run build     # tsc --noEmit + сборка в dist/
npm test          # каждая страница прогоняется в DOM
```

Файлы по разделам этого документа:

| Файл                                   | Разделы                                     |
| -------------------------------------- | ------------------------------------------- |
| [`src/types.ts`](src/types.ts)         | 2, 4 — состояния, события, контексты        |
| [`src/terminal.ts`](src/terminal.ts)   | 3.1, 5, 6 — схема терминала                 |
| [`src/host.ts`](src/host.ts)           | 3.2, 5, 6 — схема хоста                     |
| [`src/wire.ts`](src/wire.ts)           | 8 — канал; он не автомат                    |
| [`src/main.ts`](src/main.ts)           | 7, 9, 12 — стык, страница, виджеты          |

**Содержание**

1. [Постановка задачи](#1-постановка-задачи)
2. [Две машины, а не одна](#2-две-машины-а-не-одна)
3. [Графы переходов](#3-графы-переходов)
4. [Контексты](#4-контексты)
5. [Условия](#5-условия)
6. [Операции](#6-операции)
7. [Стык: Λ становится Σ](#7-стык-λ-становится-σ)
8. [Канал](#8-канал)
9. [Работа из браузера](#9-работа-из-браузера)
10. [Прогон машин](#10-прогон-машин)
11. [Анализ схем](#11-анализ-схем)
12. [Машины на странице](#12-машины-на-странице)

## 1. Постановка задачи

Задача: платёжный терминал принимает сумму и просит хост авторизации списать её. У хоста есть остаток, и в ответ приходит согласие или отказ. Между ними сеть, и сеть ненадёжна — сообщение идёт произвольное время, доставляется дважды или не доходит вовсе.

Три свойства должны выполняться при любом поведении сети.

Ответ относится к одному запросу. Терминал не принимает ответ на прошлую попытку за ответ на текущую и не считает один ответ, пришедший дважды, за два события.

Запрос списывает один раз. Если сеть доставила один и тот же запрос дважды, остаток меняется однократно. На вторую доставку приходит ответ — отправитель не отличает её от первой, — но больше ничего не происходит.

Ни одна машина не зависает из-за другой. Терминал, чей запрос потерян, остаётся там, где стоит, и показывает это; он не подставляет ответ и не переходит в состояние, для которого нет основания.

Ничто из этого не относится к сети. Всё относится к тому, какие события каждая машина принимает там, где она стоит, — а это и есть содержание схемы переходов.

## 2. Две машины, а не одна

Пару можно было бы записать одной машиной над объединением обоих множеств состояний. Здесь она записана двумя, и причины стоит назвать до кода.

Первая причина — общего контекста нет. Терминал знает номер карты и сумму, хост — остаток и список выданных ответов. Ни одному из них не нужны поля другого, а единый `Q` внёс бы оба набора в каждое состояние.

Вторая причина — канал. Переход одной машины атомарен: `dispatch` возвращает управление после того, как весь шаг выполнен (README, «Выполнение перехода: `dispatch` и `can`»). У двух машин, разделённых сетью, такого шага нет — запрос уходит из одной и приходит в другую позже или не приходит. Этот промежуток — не состояние ни одной из них.

Поэтому стык состоит ровно из одного: выходной алфавит Λ терминала переносится во входной алфавит Σ хоста и обратно.

Таблица 1 — Что пересекает канал

| Сообщение | Откуда   | Что несёт                 | Чем становится      |
| --------- | -------- | ------------------------- | ------------------- |
| `auth`    | терминал | `{ ticket, pan, amount }` | событие `auth` хоста |
| `said`    | хост     | `{ ticket, ok, why }`     | событие `said` терминала |

`ticket` есть в обоих. Это единственный способ сопоставить ответ с запросом; порядок прихода сообщений не используется ни одной из машин.

## 3. Графы переходов

### 3.1 Терминал

Таблица 2 — Состояния терминала

| Состояние  | Значение                                                    |
| ---------- | ----------------------------------------------------------- |
| `idle`     | Ничего не введено                                           |
| `entering` | В поле есть цифры                                           |
| `waiting`  | Запрос отправлен, его номер лежит в контексте               |
| `approved` | Хост списал сумму                                           |
| `declined` | Хост отказал, либо читатель прекратил ожидание              |

Таблица 3 — События терминала

| Событие  | Нагрузка    | Источник                     |
| -------- | ----------- | ---------------------------- |
| `key`    | `{ digit }` | Клавиатура                   |
| `rub`    | —           | Backspace                    |
| `send`   | —           | Клавиша отправки             |
| `said`   | `Say`       | Хост, через канал            |
| `giveUp` | —           | Читатель прекращает ожидание |
| `again`  | —           | Начать заново                |

Таблица 4 — Правила терминала

| Откуда     | Событие  | Условие    | Куда       | Выход  |
| ---------- | -------- | ---------- | ---------- | ------ |
| `idle`     | `key`    | —          | `entering` | —      |
| `entering` | `key`    | —          | `entering` | —      |
| `entering` | `rub`    | `lastOne`  | `idle`     | —      |
| `entering` | `rub`    | —          | `entering` | —      |
| `entering` | `send`   | `payable`  | `waiting`  | `auth` |
| `waiting`  | `said`   | `yes`      | `approved` | —      |
| `waiting`  | `said`   | `no`       | `declined` | —      |
| `waiting`  | `giveUp` | —          | `declined` | —      |
| `approved` | `again`  | —          | `idle`     | —      |
| `declined` | `again`  | —          | `entering` | —      |

Два отсутствия значат столько же, сколько десять правил.

В `waiting` нет правила на `key` и нет правила на `rub`. Этим и исчерпывается блокировка клавиатуры на время ожидания: `dispatch("key", …)` из `waiting` не находит ячейки, возвращает `UNHANDLED` и ничего не меняет (README, «Частичность»). Ни один флаг для этого не заводится и ни один обработчик не отключается — а в п. 9 клавиатура читает тот же факт обратно из машины.

В `waiting` два правила на `said`, и оба с условиями. Ответ, не подходящий ни под одно из них — ответ о другом номере, — не совпадает ни с одним правилом, и `dispatch` возвращает `REJECTED`. Дубликат и опоздавший ответ — это один и тот же случай, и отдельного кода для него нет.

### 3.2 Хост

Таблица 5 — Состояния хоста

| Состояние   | Значение                        |
| ----------- | ------------------------------- |
| `listening` | Простой: есть остаток и список  |
| `working`   | Идёт проверка одного запроса    |

Таблица 6 — События хоста

| Событие | Нагрузка     | Источник                   |
| ------- | ------------ | -------------------------- |
| `auth`  | `Ask`        | Терминал, через канал      |
| `ready` | `{ ticket }` | Завершение проверки        |

Таблица 7 — Правила хоста

| Откуда      | Событие | Условие  | Куда        | Выход  |
| ----------- | ------- | -------- | ----------- | ------ |
| `listening` | `auth`  | `known`  | `listening` | `said` |
| `listening` | `auth`  | —        | `working`   | —      |
| `working`   | `ready` | `afford` | `listening` | `said` |
| `working`   | `ready` | `mine`   | `listening` | `said` |

Первая ячейка — каскад условий, и порядок в нём составляет всю идемпотентность. `known` — узкий случай, и он стоит первым: на номер, ответ по которому уже выдан, ответ берётся из списка, без входа в `working` и без изменения остатка. Правило без условия под ним принимает остальное. В обратном порядке второе правило не сработало бы никогда, и `validate` сообщил бы об этом — это находка `dead-rule` (README, «`validate`»).

В `working` нет правила на `auth`. Запрос, пришедший во время проверки, отклоняется, и в п. 9 страница записывает этот отказ, а не проглатывает его.

## 4. Контексты

`Q` — носитель: состояние ↦ то, что несёт состояние (README, «Носители»). Явная запись делает поле недоступным там, где оно не имеет смысла.

```ts
export type Card = { readonly pan: string; readonly ticket: number };
export type Typed = Card & { readonly typed: string };
export type Flight = Card & { readonly amount: Cents };
export type Done = Flight & { readonly why: string };

export type TQ = Merge<
  | IState<"idle", Card>
  | IState<"entering", Typed>
  | IState<"waiting", Flight>
  | IState<"approved", Done>
  | IState<"declined", Done>
>;
```

Счётчик номеров лежит в контексте, и это не деталь оформления. Операция — чистая функция от `(context, payload)` (README, «Операции»): функция `next()`, увеличивающая переменную вне машины, дала бы для одной схемы два разных прогона, и повтор записанного прогона разошёлся бы с самим прогоном. `booked` читает `ticket + 1` (п. 6), а номер, которого машина ждёт, — это факт о состоянии, в котором она ждёт.

Поле `typed` есть в `entering` и больше нигде, поэтому нет состояния, в котором терминал держит недовведённые цифры и при этом не находится в режиме ввода. Поле `why` есть только там, где есть что показать.

Контекст хоста — два поля:

```ts
export type Ledger = {
  readonly balance: Cents;
  readonly seen: Readonly<Record<number, Say>>;
};
export type Working = Ledger & { readonly ask: Ask };
```

`seen` — список, из которого берётся ответ на вторую доставку запроса. `ask` есть только в `working`, потому что проверяемый запрос и есть содержание `working`.

## 5. Условия

Условие — чистый предикат от `(context, payload)`. Из-за чистоты `can` возвращает тот же вердикт, что и следующий `dispatch`, и ничего при этом не сдвигает (README, «`can`»).

Условия терминала:

```ts
function payable(c: Typed): boolean {
  return moneyOf(c.typed) > 0;
}

function lastOne(c: Typed): boolean {
  return c.typed.length <= 1;
}

function mine(c: Flight, p: Say): boolean {
  return p.ticket === c.ticket;
}

const yes = (c: Flight, p: Say): boolean => mine(c, p) && p.ok;
const no = (c: Flight, p: Say): boolean => mine(c, p) && !p.ok;
```

`mine` — это и есть протокол. Это не проверка, которую терминал выполняет перед выбором действия: она входит в решение о том, произойдёт ли вообще что-нибудь, и записана один раз для двух правил, которым она нужна.

Условия хоста:

```ts
function known(c: Ledger, p: Ask): boolean {
  return p.ticket in c.seen;
}

function mine(c: Working, p: { ticket: number }): boolean {
  return p.ticket === c.ask.ticket;
}

const afford = (c: Working, p: { ticket: number }): boolean =>
  mine(c, p) && c.ask.amount <= c.balance;
```

Две функции `mine` — одна и та же мысль на обоих концах: сообщение называет обмен, к которому относится, а сообщение с чужим номером ничего не меняет.

## 6. Операции

Каждая операция возвращает контекст того состояния, в которое машина входит. Там, где новое состояние несёт меньше прежнего, контекст собирается заново, а не расширяется: расширение прошло бы проверку типов и перенесло бы поле в состояние, тип которого его не содержит.

```ts
const booked = (c: Typed): Flight => ({
  pan: c.pan,
  ticket: c.ticket + 1,
  amount: moneyOf(c.typed),
});

const asked = (c: Flight) => ({
  ticket: c.ticket,
  pan: c.pan,
  amount: c.amount,
});
```

`booked` — это `with`, а `asked` — `by` того же правила. `by` выполняется на достигнутом контексте (README, «Выходные события»), поэтому номер в канале совпадает с номером в `waiting`: одно число, прочитанное один раз, в одном месте.

Операции хоста устроены так же, и одну пару стоит прочесть внимательно:

```ts
const filed = (c: Working, said: Say): Ledger => ({
  balance: said.ok ? c.balance - c.ask.amount : c.balance,
  seen: { ...c.seen, [said.ticket]: said },
});

const spoken = (c: Ledger, p: { ticket: number }): Say => c.seen[p.ticket]!;
```

`filed` записывает ответ; `spoken` — `by` обоих правил `working` — читает его обратно. Сообщение в канале не собирается второй раз рядом с записью: ответ один, лежит в одном месте, и повтор не может разойтись с оригиналом, потому что это тот же объект.

## 7. Стык: Λ становится Σ

Весь стык — шесть строк.

```ts
terminal.rx.on("auth", (ask) => {
  wire.send("down", `auth #${ask.ticket} · ${money(ask.amount)}`, () =>
    host.dispatch("auth", ask),
  );
});

host.rx.on("said", (said) => {
  wire.send("up", `${said.ok ? "ok" : "no"} #${said.ticket}`, () =>
    terminal.dispatch("said", said),
  );
});
```

Выходная буква одной машины становится входной буквой другой без изменений. Ни один модуль не импортирует другой; на оба подписана страница — единственное место, которое знает, что машин две.

Здесь нет обработки дубликата и нет обработки опоздавшего ответа. Если канал вызовет доставку дважды, `dispatch` будет вызван дважды, и второй вызов отклонят условия из п. 5. Страница узнаёт об этом из вердикта и записывает его (п. 8); машинам сообщать не нужно.

Проверка, которую выполняет хост, — это фаза, а не удерживаемое обещание:

```ts
host.rx.on(TRANSITION, ({ target }) => {
  if (target.type !== "working") return;
  const { ticket } = target.context.ask;
  setTimeout(() => host.dispatch("ready", { ticket }), 500);
});
```

`setTimeout`, а не прямой вызов, потому что вложенный в текущий `dispatch` возвращает `BUSY` (README, «Вложенный `dispatch`»). Пока проверка идёт, хост стоит в `working`, и `working` — такое же состояние на диаграмме, как остальные.

## 8. Канал

Канал — единственное на этой странице, что не автомат. Сообщение находится в нём заданное время, после чего передаётся получателю; то, что читатель делает с каналом — увеличивает время, включает вторую доставку, отбрасывает сообщение, разрывает связь, — не состояние ни одной из машин.

```ts
export type Deliver = () => Verdict;

export type Wire = {
  readonly weather: Weather;
  send(way: Way, label: string, arrive: Deliver): void;
  readonly flying: readonly Parcel[];
  watch(on: () => void): () => void;
  readonly log: readonly string[];
};
```

`Deliver` возвращает вердикт принимающей машины, и на этом держится читаемость примера. Недоставленное сообщение и доставленное, но отклонённое — разные события, и журнал их различает:

```ts
const took = arrive();
const said = `${label}${copy ? " (a copy)" : ""}`;
note(took.ok ? `▸ ${said}` : `⊘ ${said} — ${because(took)}`);
```

`because` читает ошибку вердикта — одну из пяти библиотечных (README, «Вердикт»): `UnhandledError` — правила для события там, где стоит машина, нет; `RejectedError` — ни одно условие не пропустило; `TerminalError`; `BusyError`.

Время пересечения задаётся ползунком, и от него зависит, какой из двух случаев дубликата видит читатель. Копия уходит на треть пересечения позже оригинала, проверка хоста занимает 500 мс:

| Пересечение | Разрыв между двумя | Куда попадает копия | Что происходит                            |
| ----------- | ------------------ | ------------------- | ----------------------------------------- |
| 0,2 с       | 67 мс              | `working`           | правила на `auth` там нет — отклонение    |
| 2,4 с       | 800 мс             | `listening`         | срабатывает `known` — ответ берётся из списка |

Оба случая правильны, и журнал называет оба.

## 9. Работа из браузера

Каждое действие идёт прямо в машину, без проверки фазы по дороге:

```ts
for (const key of keys)
  key.addEventListener("click", () =>
    terminal.dispatch("key", { digit: key.dataset["key"]! }),
  );
rub.addEventListener("click", () => terminal.dispatch("rub"));
send.addEventListener("click", () => terminal.dispatch("send"));
```

Каждый элемент управления включён тем же вердиктом, который вернёт следующий `dispatch`:

```ts
for (const key of keys)
  key.disabled = !terminal.can("key", { digit: key.dataset["key"]! }).ok;
rub.disabled = !terminal.can("rub").ok;
send.disabled = !terminal.can("send").ok;
giveUp.disabled = !terminal.can("giveUp").ok;
again.disabled = !terminal.can("again").ok;
```

Клавиатура гаснет в `waiting` потому, что в схеме там нет правила на `key`, а не потому, что её отключает строка кода. Уберите правило из таблицы — клавиша погаснет; добавьте — загорится. Ряд элементов управления и есть нарисованный алфавит.

## 10. Прогон машин

Один платёж на 5.00 €, канал доставляет и запрос, и ответ дважды. Канал здесь управляется вручную, поэтому порядок — записанный ниже; на странице порядок задают таймеры. Реальный вывод `log` на обеих машинах:

```text
| machine  | event    | from       | to         | output                         |
| -------- | -------- | ---------- | ---------- | ------------------------------ |
| terminal | key      | idle       | entering   | —                              |
| terminal | key      | entering   | entering   | —                              |
| terminal | key      | entering   | entering   | —                              |
| terminal | send     | entering   | waiting    | auth                           |
| host     | auth     | listening  | working    | —                              |
| host     | ready    | working    | listening  | said                           |
| host     | auth     | listening  | listening  | said                           |
| terminal | said     | waiting    | approved   | —                              |
| terminal | said     | approved   | —          | refused: UnhandledError        |

balance: 245.00 €  terminal: approved  why: charged 5.00 €
```

Три строки несут три свойства из п. 1.

Строка 7 — копия запроса. Она даёт переход `listening → listening` и выход `said`, поэтому отправитель получает ответ, а остаток равен 245.00 € — это 250.00 € минус 5.00 € один раз. Запрос списал однократно.

Строка 9 — копия ответа. К моменту её прихода терминал стоит в `approved`, где нет правила на `said`, поэтому `dispatch` возвращает `UnhandledError` и ничего не меняется. Ответ относился к одному запросу, и этот запрос закрыт.

Разорвите канал между строками 4 и 5 — строки 5 не будет: терминал останется в `waiting` и покажет это. Таймера в схеме нет, и единственный выход — `giveUp`, то есть читатель, а не часы.

## 11. Анализ схем

`validate` по обеим схемам, вывод реального прогона:

```text
===== terminal: validate =====
[]

===== host: validate =====
[]
```

Замечаний нет ни к одной: недостижимых состояний нет, правил, которые всегда перекрывает стоящее выше правило без условия, тоже нет.

`analyze`:

```text
===== terminal: analyze =====
{
  "nodes": ["idle", "entering", "waiting", "approved", "declined"],
  "reachable": ["idle", "entering", "waiting", "approved", "declined"],
  "unreachable": [],
  "terminal": []
}

===== host: analyze =====
{
  "nodes": ["listening", "working"],
  "reachable": ["listening", "working"],
  "unreachable": [],
  "terminal": []
}
```

Поле `terminal` пусто у обеих, и это утверждение о протоколе, а не о коде: ни у одной машины нет состояния, из которого нет выхода. У терминала из `approved` и `declined` ведёт `again`, хост всегда возвращается в `listening`. Платёжный терминал с тупиковым состоянием пришлось бы перезапускать.

Каскад хоста стоит проверить через `validate`, сломав его. Поставьте правило `auth` без условия первым, а правило с `known` — под ним, и `validate` сообщит о `dead-rule`: второе правило не сработает никогда, и каждый повторный запрос будет проверен и списан заново.

## 12. Машины на странице

Каждую машину рисуют виджеты инспектора, и всего, что относится к машине, здесь по два экземпляра:

```ts
const termBand = ensemble(
  fromMachine(terminal, { history: past }),
  { diagram: termDia },
  { focus: termFocus },
);
termBand.enroll(termLegend);
termBand.draw();
```

Два субъекта, две диаграммы, две легенды — по легенде под каждой машиной, чтобы оба набора состояний читались там же, где стоит машина, а не в одном ряду, который пришлось бы подписывать. Запись прогона одна, терминала: именно его фазами управляет читатель.

Панели самой страницы получают переключатель в том же ряду, что и виджеты:

```ts
for (const [name] of own) desk.seat(name);
```

`seat` регистрирует имя и рисует переключатель, но ничего не показывает — расклад панелей хранит автомат самой библиотеки, а страница читает его. Поэтому один ряд управляет всем экраном, и при выключенных переключателях на экране остаются только две машины и канал.

Док и обе легенды помечены `class="tool"`, а помеченная область показана в палитре Gruvbox, а не
в палитре страницы, и в светлой теме, и в тёмной, — то, что рисует инструмент, не спутать с тем,
что рисует приложение. Вся раскраска — блок custom properties в `shell.css`: палитра наследуется
в теневое дерево, поэтому внутрь виджета никто не лезет и ни один виджет не правится.

Где стоят панели, решает читатель, а не страница:

```ts
dockEdge(document.getElementById("board")!, "right");
```

`dockEdge` записывает край на `<body>`, а таблица стилей раскладывает док по нему — вдоль правой
стороны или по нижнему краю, если предмет широкий и низкий. Выбор запоминается для этой страницы.
